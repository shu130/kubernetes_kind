# k8s-web-app/02-db-secret.yaml
# Secret = レシピ帳（金庫保管）― DBパスワードを安全に管理

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: k8s-web-app
type: Opaque
stringData:
  POSTGRES_PASSWORD: "k8s-demo-password"
  # 本番では AWS Secrets Manager 等と連携が必須
